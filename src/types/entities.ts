export interface UserEntity {
  userName: string;
  password: string;
  email: string;
}

export interface RestaurantCategoryEntity {
  categoryName: string;
  description?: string;
}

export interface MenuItemEntity {
  menuItemName: string;
  description?: string;
}

